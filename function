# Universal functions

whatthe() {
	echo "What the..."
	echo "Wierd"
}

mkvenv() {
	DIR="$(pwd)"
	[[ -d $DIR/venv ]] && echo "Error: Virtual Environment folder already exists." && return
	python3 -m venv venv
	echo "Virtual Environment created successfully: $DIR/venv/"
}
