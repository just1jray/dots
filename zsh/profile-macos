# macOS-specific configurations
#
# Homebrew is initialized in zshenv (all shells) and re-ordered after
# path_helper in zprofile (login shells). Docker CLI completions are added
# to fpath in zshrc before compinit.
